# Casino Slots Platform

Plataforma moderna de cassino slots pronta para deploy no Vercel (frontend) e Railway (backend + PostgreSQL).

## Como rodar

### Backend

```bash
cd backend
npm install
cp .env.example .env
npm run dev
# CASSINO-NOOV
NOVO
